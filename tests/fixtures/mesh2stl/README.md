# mesh2stl Fixtures (Ticket 2)

These fixtures support spec-driven development and validation for `mesh2stl`.

- `valid/`: inputs expected to parse and convert.
- `invalid/`: inputs expected to fail deterministically by error class.

See `docs/mesh2stl-test-plan.md` for expected outcomes.
